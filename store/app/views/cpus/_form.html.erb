<%= form_for @cpu do |x| %>
  <h3> Add a CPU: </h3>
  <div style="margin-left:30px:">
    <div class="field">
      <%= x.label :name %>
      <%= x.text_field :name %>
    </div>
  </div>
  <div class="actions">
    <%= x.submit "Add CPU to list" %>
  </div>
<% end %>
